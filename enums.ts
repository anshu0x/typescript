//  it is used to set constraint values

/*

enum AuthMethod {
  push = "Push",
  sms = "SMS",
  voice = "Voice",
}

function fetchData(data: AuthMethod) {
  return data;
}

console.log(fetchData(AuthMethod.voice));

*/
